class Trip {
  final String title;
  final List<String> photos;
  final String description;
  final DateTime date;
  final String location;

  Trip(this.title, this.photos, this.description, this.date, this.location);
}